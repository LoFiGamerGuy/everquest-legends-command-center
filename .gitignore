# Never commit game-derived or private data
data/
*.sqlite
*.sqlite3
*.db
eqlog_*.txt
*.log
logs/
# Secrets
.git-credentials
.env
.env.*
# Build/deps
node_modules/
dist/
target/
.turbo/
coverage/
# OS
Thumbs.db
.DS_Store
# Fixture guard list (real names) — never commit
.forbidden-names
# Throwaway, local-only real-corpus validation scripts + their output (issue #21
# Deliverable 2). NEVER committed — they touch real logs / real player names.
packages/e2e/scripts/
